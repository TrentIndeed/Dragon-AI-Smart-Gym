# Dragon AI Smart Gym

The `dragon-gym-ui` directory contains a React touchscreen experience for the Dragon Gym dual-motor cable trainer. Launch it with:

```bash
cd dragon-gym-ui
npm install
npm start
```

The UI is optimized for 1080×1920 portrait displays with large resistance readouts, cable-length graphing, force/eccentric curve controls, and workout logging.
